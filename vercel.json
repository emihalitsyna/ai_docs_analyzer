{
  "version": 2,
  "builds": [
    { "src": "api/server.js", "use": "@vercel/node", "config": { "runtime": "nodejs20.x" } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/server.js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html", "status": 200 }
  ]
} 